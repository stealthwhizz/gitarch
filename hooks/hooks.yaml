hooks:
  # Lifecycle hooks temporarily disabled for debugging
  # Will be re-enabled once environment setup is stable
  # on_session_start:
  #   - script: validate-environment.sh
  #     description: Check required environment variables and git setup

