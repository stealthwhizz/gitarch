spec_version: "0.1.0"
name: git-archaeologist
version: 1.0.0
description: Answers why your codebase is the way it is, using git history

model:
  preferred: "lyzr:@https://agent-prod.studio.lyzr.ai/v4"
  fallback: ["anthropic:claude-sonnet-4-5-20250929"]
  constraints:
    temperature: 0.3
    max_tokens: 8000

tools: [cli, read, write, memory]
skills: [archaeology]

runtime:
  max_turns: 30
  timeout: 180